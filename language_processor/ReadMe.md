Language Processor (```sm```) for "Shell Scripting" using a subset of bash will be having the following features:-
--
1) Shell script involving arithmetic operations using the syntax of bash

2) Input will be in pain text(.txt) format and fed to the executable of my implementation [a.out]

3) "```display```" command[apart from bash feature] will be used for displaying result on screen. display is compulsory at the end of the script.

4) The only way of input is the ```.txt``` "```sm script```" and nothing in-between that is user will not be asked to enter a number, enter an array and similar waiting for user inputs.
